import styled from 'styled-components'

const StyledFooter = styled.footer`
    width: 100%;
    height: 3rem;
`

export const Footer = () => {
    return (
        <StyledFooter>
            <div>Footer</div>
        </StyledFooter>
    )
}
